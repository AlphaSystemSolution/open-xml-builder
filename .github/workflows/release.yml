name: Release

on: workflow_dispatch

permissions:
  contents: write

env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  GIT_AUTHOR_EMAIL: ${{ secrets.GIT_AUTHOR_EMAIL }}
  GIT_AUTHOR_NAME: ${{ secrets.GIT_AUTHOR_NAME }}

jobs:
  build:
    name: Build and Release
    env:
      # OSSRH/Maven Central credentials
      ORG_GRADLE_PROJECT_ossrhUsername: ${{ secrets.MAVEN_CENTRAL_USER_NAME }}
      ORG_GRADLE_PROJECT_ossrhPassword: ${{ secrets.MAVEN_CENTRAL_PASSWORD }}
      
      # GPG signing credentials
      ORG_GRADLE_PROJECT_signing_secretKey: ${{ secrets.SIGNING_IN_MEMORY_KEY }}
      ORG_GRADLE_PROJECT_signing_password: ${{ secrets.SIGNING_IN_MEMORY_KEY_PASSWORD }}
      
      # Optional: Staging profile ID
      ORG_GRADLE_PROJECT_stagingProfileId: ${{ secrets.STAGING_PROFILE_ID }}
    strategy:
      matrix:
        os: [ubuntu-latest]
        java: [temurin@21]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout current branch (full)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: '${{ github.token }}'

      - name: Setup Java (temurin@21)
        if: matrix.java == 'temurin@21'
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: "Config Git"
        run: |
          git config --global user.email "$GIT_AUTHOR_EMAIL"
          git config --global user.name "$GIT_AUTHOR_NAME"

      - name: Run Release
        run: ./gradlew release
